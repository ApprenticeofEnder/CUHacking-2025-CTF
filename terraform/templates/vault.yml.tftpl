vault_image_registry_username: "${image_registry_username}"
vault_image_registry_password: "${image_registry_password}"
vault_postgres_password: "${postgres_password}"
vault_postgres_db: "${postgres_db}"
vault_postgres_hostname: "${postgres_hostname}"
vault_postgres_user: "${postgres_user}"
vault_postgres_url: "postgres://${postgres_user}:${postgres_password}@${postgres_hostname}:5432/${postgres_db}" 
